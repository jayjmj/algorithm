/*
#include <cstdio>
#include <cstring>

int cnt[26];

int main(){

    char str[101];
    scanf("%s",str);

    int len = strlen(str);
    for(int i = 0 ; i < len ; i++) cnt[str[i]-'a']++;
    for(int i = 0 ; i < 26 ; i++) printf("%d ",cnt[i]);

    return 0;
}
*/
