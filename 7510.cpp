/*
#include <cstdio>

int main(){
    int T;
    scanf("%d",&T);

    for(int t = 1 ; t <= T ; t++){
        int a,b,c;
        scanf("%d %d %d",&a,&b,&c);
    
        bool chk = false;
        if(a*a==b*b+c*c || b*b==a*a+c*c || c*c==a*a+b*b) chk = true;
        printf("Scenario #%d:\n",t);
        if(chk) printf("yes\n\n");
        else printf("no\n\n");
    }

    return 0;
}
*/
