/*
#include <cstdio>

int main(){
	int T;
	scanf("%d",&T);

	for(int t = 1 ; t <= T ; t++){
		int a1,b1,c1,d1,a2,b2,c2,d2;
		scanf("%d %d %d %d %d %d %d %d",&a1,&b1,&c1,&d1,&a2,&b2,&c2,&d2);

		int a = a1+a2 <= 1 ? 1 : a1+a2;
		int b = b1+b2 <= 1 ? 1 : b1+b2;
		int c = c1+c2 <= 0 ? 0 : c1+c2;
		int d = d1+d2;

		printf("%d\n",a+5*b+2*c+2*d);
	}

	return 0;
}
*/
