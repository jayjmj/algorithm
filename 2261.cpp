/*
#include <cstdio>

using namespace std;

typedef struct axis{
    int x,y;
    axis(int _x,int_y){x=_x;y=_y;}
} Axis;

Axis ax[100001];

bool cmp(const Axis &a,const Axis &b){
    if(a.x == b.x) a.y < b.y;
    return a.x < b.x;
}

int main(){
    int n;
    scanf("%d",&n);

    int x,y;
    for(int i = 1 ; i <= n ; i++){
        scanf("%d %d",&x,&y);
        
    }

    return 0;
}
*/
